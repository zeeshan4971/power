@extends('layouts.app')
@php($activePage='alerts')
@section('content')<main class="main">@include('partials-top',['title'=>'Early Warning Alerts','subtitle'=>'2 Active Alerts'])<section class="content" data-tour="alerts-page"><div class="card-box"><button class="btn btn-primary">All</button><button class="btn">Active</button><button class="btn">Resolved</button></div>@foreach($student?->alerts ?? [] as $alert)<div class="card-box" style="border-color:{{ $alert->priority==='high'?'#ff6b6b':($alert->status==='resolved'?'#43ad4c':'#f2c200') }}"><h3>{{ $alert->title }}</h3><p>{{ $alert->description }}</p><small>{{ ucfirst($alert->priority) }} Priority • {{ ucfirst($alert->status) }}</small>@if($alert->status!=='resolved')<form method="POST" action="{{ route('alerts.resolve',$alert) }}" class="mt-2">@csrf<button class="btn btn-danger">Take Action / Resolve</button></form>@endif</div>@endforeach<div class="card-box"><h3>Recommended Actions</h3><ul><li>Schedule a quick check-in with Alex</li><li>Review missing assignments together</li><li>Praise recent behavior improvements</li></ul></div></section></main>@endsection
